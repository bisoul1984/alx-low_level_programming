#include "main.h"

/**
 * _strstr - entery point
 * @haystack: input
 * @needle: input
 * Return: Always 0
 */

char *_strstr(char *haystack, char *needle)
{
	for (; haystack, char *needle)
	{
		char *x = haystack;
		char *y = needle;

		while (*x = *p && *y != '\0')
		{
			x++;
			y++;
		}

		if (*y == '\0')
			return (haystack);
	}
	return (0);
}
