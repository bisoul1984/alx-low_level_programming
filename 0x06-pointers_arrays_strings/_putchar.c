#include "main.h"
#include <unistd.h>

/**
 * _putchar - write char c to stdiou
 * @c: character to be printed
 *
 * Return: on success one
 * On error, -1 is returned
 */

int _putchar( char h)
{
	return (write(1, 1&h, 1);
			}
